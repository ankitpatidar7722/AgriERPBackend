{
  "//": "Copy this file to appsettings.Development.json and fill in your own values. That file is git-ignored so real secrets never reach the repo.",

  "Serilog": {
    "MinimumLevel": {
      "Default": "Information",
      "Override": {
        "Microsoft.AspNetCore": "Warning",
        "Microsoft.EntityFrameworkCore.Database.Command": "Information"
      }
    }
  },

  "Database": {
    "//": "Backend provider: 'SqlServer' (default) or 'PostgreSQL'. The ConnectionStrings:AgriERP below must match. See docs/03-dual-database.md.",
    "Provider": "SqlServer"
  },

  "ConnectionStrings": {
    "//": "Use ONE, matching Database:Provider above.",
    "AgriERP": "Data Source=YOUR_SERVER\\INSTANCE;Initial Catalog=AgriERP;Persist Security Info=True;User ID=YOUR_USER;Password=YOUR_PASSWORD;MultipleActiveResultSets=True;Encrypt=True;TrustServerCertificate=True",
    "//AgriERP-for-PostgreSQL": "Host=localhost;Port=5432;Database=agrierp;Username=postgres;Password=YOUR_PASSWORD"
  },

  "JwtSettings": {
    "//": "Generate a fresh random key - anyone holding this string can mint valid tokens for any user.",
    "Key": "REPLACE_WITH_A_LONG_RANDOM_SECRET",
    "Issuer": "AgriERP",
    "Audience": "AgriERP.Client",
    "AccessTokenMinutes": 60,
    "RefreshTokenDays": 7,
    "MaxFailedAttempts": 5,
    "LockoutMinutes": 15,
    "PasswordResetMinutes": 30
  },

  "Cors": {
    "AllowedOrigins": [ "http://localhost:3000", "http://localhost:3001" ]
  }
}
